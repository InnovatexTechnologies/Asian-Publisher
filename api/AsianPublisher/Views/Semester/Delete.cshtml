@model AsianPublisher.Models.Semester

@{
    ViewData["Title"] = "Delete";
}
@if (!string.IsNullOrEmpty(ViewBag.error))
{
    <div class="alert alert-danger">
        @ViewBag.error
    </div>
}
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Semester</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
           Name
        </dt>
        <dd class="col-sm-10">
            @Model.name
        </dd>
        <dt class="col-sm-2">
            Alias
        </dt>
        <dd class="col-sm-10">
            @Model.alias
        </dd>
        <dt class="col-sm-2">
           Academic Year
        </dt>
        <dd class="col-sm-10">
            @Model.academicNav?.name
        </dd>
        <dt class="col-sm-2">
           Semester Category
        </dt>
        <dd class="col-sm-10">
            @Model.semesterNav?.name
        </dd>
    </dl>

    <form asp-action="Delete">
            <input name = "url" hidden value = "@ViewBag.url" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a href="@ViewBag.url">Back to List</a>
    </form>
</div>
